{
  "name": "example",
  "version": "0.0.1",
  "description": "example project for testing frontend-maven-plugin",
  "dependencies": {
    "jquery": "3.6.0"
  },
  "scripts": {
    "prebuild": "mkdir target/npm-build",
    "build": "echo foobar > target/npm-build/result.txt"
  },
  "devDependencies": {
    "@esm-bundle/chai": "^4.3.4-fix.0",
    "@web/test-runner": "^0.14.0",
    "vite": "^3.1.6"
  }
}